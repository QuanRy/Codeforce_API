python -m venv venv

venv\Scripts\activate

pip install -r requirements.txt

# поднять FRONTEND (внутри папки frontend)
python -m http.server --directory frontend 7000 

# поднять FASTAPI-сервисы
uvicorn backend.codeforce_ID:app --reload --host 0.0.0.0 --port 8000

# Адрес фронта
http://localhost:7000/